# Pieces Directory

This directory contains tabletop piece components for Protoboard.

## Directory Inventory

- [`d1.ts`](./d1.ts): Single-faced piece component (`D1`) rendering a single projected slot (`face0`) and supporting the `pick` action.
