# Testing Directory

This directory contains test-only export modules and helper components for Protoboard test fixtures.

## Directory Inventory

- [`index.ts`](./index.ts): Test entry point exporting internal services and registration functions for test harnesses.
- [`test-face.ts`](./test-face.ts): Test-only piece/card face component (`TestFace`).
